# 全局编译配置
# 注意：不在 [build] 中设置 rustflags，因为会影响 build scripts 编译
# 性能优化通过 Cargo.toml 的 [profile.release] 实现

[target.x86_64-pc-windows-msvc]
# Windows 平台无需特殊 linker 配置

[target.x86_64-unknown-linux-gnu]
linker = "gcc"
