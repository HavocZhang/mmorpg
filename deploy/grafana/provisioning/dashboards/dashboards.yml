apiVersion: 1

providers:
  - name: "MMO Gateway Dashboards"
    orgId: 1
    folder: "MMO Gateway"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
